import * as React from 'react';

interface IRoadmapProps {
  Roadmap: object
}

const Roadmap: React.SFC<IRoadmapProps> = (props) => {
  return (
    <React.Fragment>
      Roadmap
      </React.Fragment>
  );
};

export default Roadmap;