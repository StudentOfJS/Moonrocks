import * as React from 'react';

interface IWhitepaperProps {
  Whitepaper: object
}

const Whitepaper: React.SFC<IWhitepaperProps> = (props) => {
  return (
    <React.Fragment>
      Whitepaper
      </React.Fragment>
  );
};

export default Whitepaper;